import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  root: {
    alignItems: "center",
    padding: 20,
  },
  title: {
    fontSize: 24,
    marginVertical: 50,
    fontWeight: "700",
    color: "#051C60",
  },
});

export default styles;
