import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    root: {
      alignItems: "center",
      padding: 20,
    },
    title: {
      fontSize: 24,
      fontWeight: "700",
      color: "#051C60",
      marginVertical: 50,
    },
  });

  export default styles